
var Tuiter = require('./');


var t = new Tuiter(require('./test/keys.json'));


t.filter({follow: ['308711490']}, function(stream){
  stream.on('tweet', function(data){
    console.log(data.text);
  });

  stream.on('error', function(er){
    console.log(er);
  });
});
